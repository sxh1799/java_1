1、哈希表
    JDK8之前，哈希表=数组+链表【拉链法】
    JDK8开始，哈希表=数组+链表+红黑树 【即当拉链长度达到8，且数组长度>64，会把拉链转为红黑树】
    哈希表是一种增删改查数据，性能都较好的数据结构，但是它是无序，不能重复，没有索引支持的(由键决定特点)
    HashMap的键依赖hashCode方法和equals方法保证键的唯一
    如果键存储的是自定义类型的对象，可以通过重写对象的hashCode和equals方法（快捷键生成即可）
    这样可以保证多个对象内容一样时，HashMap集合就能认为是重复的